import { render } from '@testing-library/react';
import '@testing-library/jest-dom';

import SignUp from '../index';

const mockProps = {};

describe('', () => {
  // const wrapper = render(<SignUp {...mockProps} />);

  test('render()', async () => {
    // expect(wrapper.length).toEqual(1);
  });
});
